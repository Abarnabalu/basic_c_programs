#include <stdio.h>

int main() {
    int num = 5; // Initialize an integer variable with the value 42

    // Print the initialized integer
    printf("The value is: %d\n", num);

    return 0;
}
